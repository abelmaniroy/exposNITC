int main(){
	decl
	int t,ret;
	str username,passwd;
	int word;
	enddecl
	begin
	
	
	while(1==1) do
		t=exposcall("Write",-2,"Username:");
		t=exposcall("Read",-1,username);
		t=exposcall("Write",-2,"Password:");
		t=exposcall("Read",-1,passwd);
		
		ret=exposcall("Login",username,passwd);
		
		if(ret==-1) then
			t=exposcall("Write",-2,"Invalid");
		else
			if(ret==-2) then
				t=exposcall("Write",-2,"permiss denied");
			endif;
		endif;
		
	endwhile;
	
	return 0;
	end
}
